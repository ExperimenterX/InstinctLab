import { NotImplemented } from "@instinct/shared";

/**
 * What the learner opens tomorrow, and tomorrow is the whole point of the product.
 *
 * Recall card, session summary, the predictions they missed, and a one-question retention check.
 * Keep it to one screen with no scrolling on a laptop.
 */
export function RecapScreen(_props: { sessionId: string }): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/RecapScreen");
}

import { stopLossNode } from "../../../concepts/stop-loss/node.js";

/**
 * The renderer's standalone example, taken from the stop-loss concept node so the two cannot
 * drift apart — if one breaks, both do, loudly. Same arrangement as structure-diagram.
 */
export const MARKET_TAPE_FIXTURE = stopLossNode.spec;

import { orderBookNode } from "../../../concepts/order-book/node.js";

/**
 * The renderer's standalone example, taken from the order-book concept node so the two cannot
 * drift apart — if one breaks, both do, loudly.
 */
export const DEPTH_LADDER_FIXTURE = orderBookNode.spec;

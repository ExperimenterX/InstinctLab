import { NotImplemented } from "@instinct/shared";

/**
 * Three to five items built from this specific session. Label the provenance honestly: telling\n * the learner they predicted this one wrong measurably beats an unlabelled review question.
 */
export function QuizPanel(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/QuizPanel");
}

import { NotImplemented } from "@instinct/shared";

/**
 * The flagship quiz item: the learner drives the real lab to hit a target.\n *\n * Reuses the live canvas and knob panel rather than a mock, which is the entire point. Producing\n * an outcome demonstrates transferable intuition; recognising a sentence demonstrates reading.\n *\n * Grading happens server-side by re-running the sim. Do not grade locally.
 */
export function TuneItem(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/TuneItem");
}

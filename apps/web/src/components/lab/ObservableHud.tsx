import { NotImplemented } from "@instinct/shared";

/**
 * Read-outs from useObservable, which serves 10Hz samples. Never reads the slab directly.\n *\n * Format per the spec declared format and precision. Show the good direction as a supporting cue,\n * never as colour alone.
 */
export function ObservableHud(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/ObservableHud");
}

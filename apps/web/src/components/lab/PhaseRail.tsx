import { NotImplemented } from "@instinct/shared";

/**
 * See, Interact, Experiment, Predict, Understand, Recall, with goal progress.\n *\n * Completed beats stay clickable: the learner may always scrub back and keep playing, and goals\n * never re-lock.
 */
export function PhaseRail(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/PhaseRail");
}

import { NotImplemented } from "@instinct/shared";

/**
 * Named starting points from the current beat.\n *\n * Labels must read as experiments, never as answers: turn off damping, not the correct setting.
 */
export function PresetRow(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/PresetRow");
}

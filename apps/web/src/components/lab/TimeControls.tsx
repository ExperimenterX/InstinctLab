import { NotImplemented } from "@instinct/shared";

/**
 * Play, pause, step, scrub, speed. Hidden until a beat unlocks time.\n *\n * Scrub reads the snapshot ring, so its range is bounded by ring capacity. Show that bound\n * honestly rather than implying a longer history than exists.
 */
export function TimeControls(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/TimeControls");
}

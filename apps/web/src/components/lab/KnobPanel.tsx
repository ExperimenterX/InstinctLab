import { NotImplemented } from "@instinct/shared";

/**
 * One knob per unlocked param. The primary knob renders large: during INTERACT it is the only\n * thing on screen the learner can touch, and that single moment establishes the contract that\n * the simulation obeys them.
 */
export function KnobPanel(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/KnobPanel");
}

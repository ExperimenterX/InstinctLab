import { NotImplemented } from "@instinct/shared";

/**
 * Transient, bottom-anchored, at most one message visible.\n *\n * It appears AFTER the learner acts, and it must never block the canvas or steal focus: their\n * hand may still be on a knob.
 */
export function CoachDock(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/CoachDock");
}

import { NotImplemented } from "@instinct/shared";

/**
 * Generation progress, shown while the lab composes.\n *\n * Must never look like an error and must never be a blank screen. The plan arrives at around\n * 1.2s carrying the title and teaching angle, so there is something honest to show well before\n * the canvas goes live.
 */
export function GenerationSkeleton(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/GenerationSkeleton");
}

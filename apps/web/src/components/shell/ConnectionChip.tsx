import { NotImplemented } from "@instinct/shared";

/**
 * Connection state. A subtle chip, never a modal.\n *\n * The simulation is local, so a dropped stream costs the learner the coach, not the lab. They\n * can keep playing.
 */
export function ConnectionChip(): JSX.Element {
  // TODO(FRONTEND)
  throw new NotImplemented("web/ConnectionChip");
}

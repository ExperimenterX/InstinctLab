import { NotImplemented } from "@instinct/shared";

/**
 * See docs/05-api-contract.md for the request/response shapes this plugin must satisfy.
 * Validate every body with the lab-schema Zod schema BEFORE doing anything else.
 */
export async function recapRoutes(_app: unknown): Promise<void> {
  // TODO(API)
  throw new NotImplemented("api/recapRoutes");
}

{
  "name": "@instinct/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@instinct/ai-core": "workspace:*",
    "@instinct/lab-schema": "workspace:*",
    "@instinct/lab-sim": "workspace:*",
    "@instinct/persistence": "workspace:*",
    "@instinct/shared": "workspace:*",
    "fastify": "^5.2.0",
    "zod": "^3.23.8"
  }
}
